<div class="admin_info">
    <% if current_admin.league_url.size != 0 %>
   		<%= link_to league_logo, root_path %>
    <% else %>
    	<small>(set league logo image URL in 'Settings')</small>
    <% end %>

     <span class="admin_name">
      </br>
      <%= current_admin.name %>
    </span>

	<% unless current_admin.challenges.empty? %>
		<% @challenges = current_admin.challenges %>
  		<b>Challenges</b>
  		<table class="challenges" summary="league challenges">
  		<%= render @challenges %>
  		</table>

  <nav class="round">
    <ul>
      <li><%= link_to 'On-Ramp', :controller => 'admins', :method => :get, :action => 'onramp' %>
      <li><%= link_to 'Engagement', :controller => 'admins', :method => :get, :action => 'engagement' %>
      <li><%= link_to 'Graphs', :controller => 'admins', :method => :get, :action => 'graphs' %>
    </ul>
  </nav>

	<% end %>

  </a>
</div>
